apiVersion: v1
kind: Service
metadata:
  name: mcp-ui-demo
  namespace: mcp-ui
  labels:
    app: mcp-ui-demo
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: mcp-ui-demo
