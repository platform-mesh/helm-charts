name: Build platform-mesh-operator-infra-components Workflow
on:
  push:
    branches:
      - main
    paths:
      - 'charts/platform-mesh-operator-infra-components/**'
      - '.github/workflows/platform-mesh-operator-infra-components.yaml'
      - '.ocm/component-constructor-chart-only.yaml'

permissions:
  contents: write
  packages: write

jobs:
  pipeline:
    concurrency:
      group: platform-mesh-operator-infra-components-${{ github.ref }}
      cancel-in-progress: true
    uses: ./.github/workflows/pipeline-chart.yml
    with:
      chartFolder: charts
      chartName: platform-mesh-operator-infra-components
      componentConstructorFile: .ocm/component-constructor-chart-only.yaml
      chartOnly: true
    secrets: inherit

