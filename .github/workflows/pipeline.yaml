name: pipeline
on:
  push:
    paths:
      - 'local-setup/**'
      - '!**'
      - 'local-setup/**'
  # pull_request:
  #   branches:
  #     - main
    # paths:
    #   - 'local-setup/**'
    #   - '!**'
    #   - 'local-setup/**'

concurrency:
  group: localsetup-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:

  # pipeline:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: lint
  #       id: lint
  #       run: |
  #         echo "No-op"
  #     - name: unittest
  #       id: unittest
  #       run: |
  #         echo "No-op"

  testChart:
    uses: ./.github/workflows/noop-lint.yml
