name: Build platform-mesh-operator Workflow
on:
  push:
    branches:
      - main
    paths:
      - 'charts/platform-mesh-operator/**'
      - '.github/workflows/platform-mesh-operator.yaml'
      - '.ocm/component-constructor.yaml'
      - 'rgd/platform-mesh-operator-rgd.yaml'
  pull_request:
    paths:
      - 'charts/platform-mesh-operator/**'
      - '.github/workflows/platform-mesh-operator.yaml'
      - 'rgd/platform-mesh-operator-rgd.yaml'

permissions:
  contents: read
  packages: write

jobs:
  pipeline:
    concurrency:
      group: platform-mesh-operator-${{ github.ref }}
      cancel-in-progress: true
    uses: platform-mesh/.github/.github/workflows/pipeline-chart.yml@5c377e5944acf613950dfe83a13cc7ab3054728f # main
    with:
      chartFolder: charts
      chartName: platform-mesh-operator
      additionalTestFilesCommand: ''
      chartRepos: 'bitnami=https://charts.bitnami.com/bitnami,openfga=https://openfga.github.io/helm-charts'
      componentConstructorFile: .ocm/component-constructor-platform-mesh-operator.yaml
    secrets: inherit

