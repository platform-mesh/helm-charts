name: Build common Workflow
on:
  push:
    branches:
      - main
    paths:
      - 'charts/common/**'
      - '.github/workflows/common.yaml'
  pull_request:
    paths:
      - 'charts/common/**'
      - '.github/workflows/common.yaml'

concurrency:
  group: common-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipeline:
    uses: platform-mesh/.github/.github/workflows/pipeline-chart.yml@5c377e5944acf613950dfe83a13cc7ab3054728f # main
    with:
      chartFolder: charts
      chartName: common
      additionalTestFilesCommand: ''
      chartRepos: 'bitnami=https://charts.bitnami.com/bitnami,openfga=https://openfga.github.io/helm-charts'
      disableOCM: true
    secrets: inherit
