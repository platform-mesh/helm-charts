name: Build infra Workflow
on:
  push:
    branches:
      - main
    paths:
      - 'charts/infra/**'
      - '.github/workflows/infra.yaml'
      - '.ocm/component-constructor-chart-only.yaml'
  pull_request:
    paths:
      - 'charts/infra/**'
      - '.github/workflows/infra.yaml'

permissions:
  contents: read
  packages: write

jobs:
  pipeline:
    concurrency:
      group: infra-${{ github.ref }}
      cancel-in-progress: true
    uses: platform-mesh/.github/.github/workflows/pipeline-chart.yml@f3aaa6f289ab188f46371a5bf9da6f0897038861 # main
    with:
      chartFolder: charts
      chartName: infra
      additionalTestFilesCommand: ''
      chartRepos: 'bitnami=https://charts.bitnami.com/bitnami,openfga=https://openfga.github.io/helm-charts'
      componentConstructorFile: .ocm/component-constructor-chart-only.yaml
    secrets: inherit

