{{- if .Values.etcdDruid.enabled }}
apiVersion: delivery.ocm.software/v1alpha1
kind: Resource
metadata:
  labels:
    artifact: chart
    repo: git
  name: {{ .Values.etcdDruid.name }}
spec:
  componentRef:
    name: {{ .Values.ocm.component.name }}
  interval: {{ .Values.ocm.interval }}
  ocmConfig:
    - apiVersion: delivery.ocm.software/v1alpha1
      kind: Repository
      name: {{ .Values.ocm.repo.name }}
      namespace: {{ .Release.Namespace }}
  resource:
    byReference:
      referencePath:
      {{- range .Values.ocm.referencePath }}
        - name: {{ . }}
      {{- end }}
        - name: {{   .Values.etcdDruid.name }}
      resource:
        name: {{   .Values.etcdDruid.ocmResourceName }}
        type: helmChart/v1
  skipVerify: {{ .Values.ocm.skipVerify }}
{{- end }}