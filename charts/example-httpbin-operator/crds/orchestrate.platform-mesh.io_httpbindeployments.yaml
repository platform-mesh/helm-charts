---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: httpbindeployments.orchestrate.platform-mesh.io
spec:
  group: orchestrate.platform-mesh.io
  names:
    kind: HttpBinDeployment
    listKind: HttpBinDeploymentList
    plural: httpbindeployments
    singular: httpbindeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .spec.service.type
      name: Service Type
      type: string
    - jsonPath: .spec.service.port
      name: Port
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HttpBinDeployment is the Schema for the httpbindeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HttpBinDeploymentSpec defines the desired state of HttpBinDeployment
            properties:
              deployment:
                description: Deployment defines the deployment configuration for HttpBin
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to be added to the deployment and pods
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to be added to the deployment and pods
                    type: object
                  name:
                    description: Name is the name of the deployment. If not provided,
                      the HttpBinDeployment name will be used.
                    type: string
                  replicas:
                    default: 1
                    description: Replicas is the number of desired replicas
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              service:
                description: Service defines the service configuration for the HttpBin
                  deployment
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to be added to the service
                    type: object
                  name:
                    description: Name is the name of the service. If not provided,
                      the HttpBinDeployment name will be used.
                    type: string
                  port:
                    default: 80
                    description: Port is the port on which the service will listen
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  type:
                    default: ClusterIP
                    description: |-
                      Type determines how the Service is exposed. Defaults to ClusterIP.
                      Valid values are:
                      - "ClusterIP": Exposes the service on a cluster-internal IP (default)
                      - "NodePort": Exposes the service on each Node's IP at a static port
                      - "LoadBalancer": Exposes the service externally using a cloud provider's load balancer
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
            type: object
          status:
            description: HttpBinDeploymentStatus defines the observed state of HttpBinDeployment
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              isDeploymentReady:
                description: IsDeploymentReady indicates if the deployment is ready
                  (all replicas are available)
                type: boolean
              readyReplicas:
                description: ReadyReplicas is the number of pods targeted by this
                  HttpBin deployment with a Ready Condition
                format: int32
                type: integer
              url:
                description: |-
                  URL is the HTTPS URL for accessing the httpbin service
                  Format: https://HOST
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
