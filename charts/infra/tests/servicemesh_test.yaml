suite: Test service-mesh manifests
templates:
  - keycloak/httproute.yaml
  - keycloak/db-secret.yaml
  - keycloak/admin-secret.yaml
  - keycloak/keycloak.yaml
  - gatewayapi/gateway.yaml
release:
  name: infra
  namespace: platform-mesh-system
tests:
- it: test HTTPRoutes
  templates:
  - keycloak/httproute.yaml
  set:
    gatewayApi:
      enabled: true
    cors:
      enabled: true
  asserts:
  - matchSnapshot: {}
- it: configure Gateway infrastructure
  templates:
  - gatewayapi/gateway.yaml
  set:
    gatewayApi:
      enabled: true
      infrastructure:
        annotations:
          dns.gardener.cloud/class: garden
  asserts:
  - equal:
      path: spec.infrastructure.annotations
      value:
        dns.gardener.cloud/class: garden
- it: keycloak operator disabled by default
  templates:
  - keycloak/db-secret.yaml
  - keycloak/admin-secret.yaml
  - keycloak/keycloak.yaml
  asserts:
  - hasDocuments:
      count: 0
- it: keycloak operator enabled renders all resources
  templates:
  - keycloak/db-secret.yaml
  - keycloak/admin-secret.yaml
  - keycloak/keycloak.yaml
  set:
    keycloak:
      operator:
        enabled: true
        image:
          tag: "mail"
  asserts:
  - matchSnapshot: {}
