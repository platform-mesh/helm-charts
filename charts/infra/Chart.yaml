apiVersion: v2
name: infra
description: The infra platform-mesh chart configures a number of common infrastructure components for the Platform Mesh platform.
type: application
version: 0.63.4
appVersion: "0.0.0"

dependencies:
  - name: common
    version: 0.5.6
    repository: file://../common
