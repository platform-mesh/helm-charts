suite: crds
templates:
  - gateway.networking.k8s.io_gatewayclasses.yaml
values:
  - ../test-values.yaml
chart:
  version: 0.1.0
  appVersion: v1.4.1
tests:
  - it: GatewayClass CRD matches snapshot
    asserts:
      - matchSnapshot: {}
