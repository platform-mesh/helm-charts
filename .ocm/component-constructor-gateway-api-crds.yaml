components:
  - name: ${COMPONENT_NAME}
    version: ${VERSION}
    provider:
      name: The Platform Mesh Team
    resources:
      - name: chart
        type: helmChart
        relation: external
        version: ${VERSION}
        access:
          type: ociArtifact
          imageReference: "${CHART_OCI_PATH}:${VERSION}"
