apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: prometheusinstances.monitoring.provider.example.com
spec:
  group: monitoring.provider.example.com
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                clusterRef:
                  type: object
                  properties:
                    name:
                      type: string
                      description: "Name of the referenced cluster."
                    namespace:
                      type: string
                      description: "Namespace of the referenced cluster."
                  description: "Namespaced reference to a cluster resource."
                config:
                  type: object
                  additionalProperties:
                    type: string
                  description: "Arbitrary configuration key-value pairs for PrometheusInstance."
                enabled:
                  type: boolean
                  description: "Whether this PrometheusInstance is enabled."
                description:
                  type: string
                  description: "Description of this PrometheusInstance."
  scope: Namespaced
  names:
    plural: prometheusinstances
    singular: prometheusinstance
    kind: PrometheusInstance
    listKind: PrometheusInstanceList
