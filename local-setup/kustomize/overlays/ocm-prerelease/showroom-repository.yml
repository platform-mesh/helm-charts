apiVersion: delivery.ocm.software/v1alpha1
kind: Repository
metadata:
  name: showroom
  namespace: default
spec:
  interval: 1m0s
  ocmConfig:
  - kind: Secret
    name: ocm-oci-apeiro-pull
    policy: Propagate
  repositorySpec:
    baseUrl: http://oci-registry-docker-registry.registry.svc.cluster.local:5000/platform-mesh
    type: OCIRegistry
