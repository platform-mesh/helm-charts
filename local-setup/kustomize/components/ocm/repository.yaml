apiVersion: delivery.ocm.software/v1alpha1
kind: Repository
metadata:
  name: platform-mesh
spec:
  repositorySpec:
    baseUrl: ghcr.io/platform-mesh
    type: OCIRegistry
  ocmConfig:
    - apiVersion: v1
      kind: ConfigMap
      name: ocm-resolver-config
      namespace: default
      policy: Propagate
  interval: 1m
