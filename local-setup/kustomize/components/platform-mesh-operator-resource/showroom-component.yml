apiVersion: delivery.ocm.software/v1alpha1
kind: Component
metadata:
  name: showroom
  namespace: default
spec:
  component: github.tools.sap/apeiro/showroom
  downgradePolicy: Deny
  interval: 1m0s
  ocmConfig:
  - apiVersion: delivery.ocm.software/v1alpha1
    kind: Repository
    name: showroom
    namespace: default
    policy: Propagate
  repositoryRef:
    name: showroom
  semver: 0.0.16
