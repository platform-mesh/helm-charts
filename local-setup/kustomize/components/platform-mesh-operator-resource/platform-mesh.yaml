apiVersion: core.platform-mesh.io/v1alpha1
kind: PlatformMesh
metadata:
  name: platform-mesh
  namespace: platform-mesh-system
spec:
  exposure:
    port: 8443
    baseDomain: "portal.localhost"
    protocol: "https"
    traefikClusterIP: "10.96.188.4"
    kcpFrontProxyClusterIP: "10.96.0.100"
  ocm:
    repo:
      name: platform-mesh
    component:
      name: platform-mesh
    referencePath: []
  featureToggles:
  - name: feature-enable-getting-started
  - name: feature-disable-email-verification
  - name: feature-accounts-in-accounts
  - name: feature-enable-provider-permissions
  kcp: {}
  values:
    platform-mesh-operator:
      values:
        # TEMPORARY: pinned to an unreleased commit from PR #333 (core.platform-mesh.io
        # split), not merged yet, revert once it lands on main.
        image:
          tag: "753a87b0"
    security-operator:
      values:
        # TEMPORARY: pinned to an unreleased commit from PR #333 (core.platform-mesh.io
        # split), not merged yet, revert once it lands on main.
        image:
          tag: "753a87b0"
